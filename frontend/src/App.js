//const SearchLabel = document.querySelector(".search");
//const SearchWrapper = document.querySelector(".search-wrapper");

/* let SearchInput;

// toggle search input
function toggleSearchInput() {
  if (SearchInput) return;

  SearchInput = document.createElement("input");
  SearchInput.className =
    "px-4 py-1 text-sm border border-gray-400 rounded-2xl focus:border-gray-400 focus:outline-0";
  SearchInput.placeholder = "search...";

  SearchWrapper.appendChild(SearchInput);
  SearchInput.focus();

  function clickOutside(e) {
    if (SearchLabel && !SearchWrapper.contains(e.target)) {
      SearchInput.remove();
      SearchInput = null;
      document.addEventListener("click", clickOutside);
    }
  }
  document.addEventListener("click", clickOutside);
}

SearchLabel.addEventListener("click", toggleSearchInput); */


